import React from "react";
import { Text, StyleSheet } from "react-native";

const ScoreScreen = () => {
    return <Text style={styles.text}>Score Screen</Text>;
};

const styles = StyleSheet.create({
    text: {
        fontSize: 30
    }
});

export default ScoreScreen;
