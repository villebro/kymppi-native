import React from "react";
import { Text, StyleSheet } from "react-native";

const ActivitiesScreen = () => {
    return <Text style={styles.text}>Activities Screen</Text>;
};

const styles = StyleSheet.create({
    text: {
        fontSize: 30
    }
});

export default ActivitiesScreen;
